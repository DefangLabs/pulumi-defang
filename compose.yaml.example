services:
  app:
    restart: unless-stopped
    image: redis:latest
    ports:
      - mode: ingress
        target: 6379
        published: 6379
    deploy:
      resources:
        reservations:
          memory: 256M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
