name: defang-gcp-go
runtime: go
description: Example using defang-gcp to deploy services to GCP (Pulumi Go)
plugins:
  providers:
    - name: defang-gcp
      path: ../../bin
config:
  gcp:project: my-project-123456
  domain: gcp-go-my-project.example.defang.app
